

#|

pfnum
prime factored numbers 

|#

(define make-pf (lambda (n) (factor n)))

(define pf-add (lambda (a b)
		 (cond
		  (( ........  )))))


(define pf-mul (lambda (a b)
		 (cond
		  (( ........  )))))


(define pf-divisible-by (lambda (a b)
			  (cond
			   (( ........  )))))




